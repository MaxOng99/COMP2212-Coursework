Int[][] in = sequences
Int[] shuffleList = []
do shuffleList.push(0)

if (not in.empty) {
	Int[] firstList = in.pop
	Int[] secondList = in.pop

	Int a
	Int b
	Int c 
	Int d

	while (not secondList.empty) {
		do secondList.pop

		if (not secondList.empty) {
			a = secondList.pop
			b = firstList.pop
			do shuffleList.push(a)
			do shuffleList.push(b)
		}
	}

}
return shuffleList