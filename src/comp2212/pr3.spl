Int[][] in = sequences
Int[] firstList = in.pop
Int[] secondList = in.pop
Int[] shuffleList = []
Int a
Int b
Int c 
Int d
if (secondList.length >= 2) {
	do shuffleList.push(0)
	do secondList.pop
	a = secondList.pop
	b = firstList.pop
	do shuffleList.push(a)
	do shuffleList.push(b)
	while (secondList.length >= 2) {
		do secondList.pop
		c = secondList.pop
		d = firstList.pop
		do shuffleList.push(c)
		do shuffleList.push(d)
	}
}
return shuffleList