Int[][] in = sequences
Int[] firstList = in.pop
int[] secondList = in.pop
Int[] shuffleList = []

if (secondList.size >= 2) {
	shuffleList.push(0)
	
	secondList.pop

	Int a = secondList.pop
	Int b = firstList.pop
	shuffleList.push(a)
	shuffleList.push(b)

	while (secondList.size >= 3) {
		
		secondList.pop

		Int c = secondList.pop
		Int d = firstList.pop

		shuffleList.push(c)
		shuffleList.push(d)
	}
}

return shuffleList
