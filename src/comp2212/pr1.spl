Int[][] in = sequences
Int[] shuffle = []

if (not (in.empty)) {
	Int[] first = in.pop
	Int[] second = in.pop
	Int x
	Int y
	Int z

	while (not first.empty) {
		x = first.pop
		do shuffle.push(x)
		if (not first.empty) {
			y = first.pop
			z = second.pop
			do shuffle.push(y)
			do shuffle.push(z)
		}
	}
}
return shuffle