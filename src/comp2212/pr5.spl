Int[][] inputList = sequences
Int[] firstList = inputList.pop
Int[] outputList = []
Int sum
Int a
while (not firstList.empty) {
	a = firstList.pop
	sum = sum + a
	do outputList.push(sum)
}
return outputList