Int[][] in = sequences
Int[] outputList = []

if (not in.empty) {
	Int[] firstList = in.pop

	Int sum
	Int a
	while (not firstList.empty) {
		a = firstList.pop
		sum = sum + a
		do outputList.push(sum)
	}
}
return outputList