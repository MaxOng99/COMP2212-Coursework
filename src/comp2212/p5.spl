Int[][] inputList = sequences
Int[] firstList = inputList.pop
Int[] outputList = []

Int sum = 0

while (not firstList.empty) {

	Int a = firstList.pop
	sum = sum + a
	outputList.push(sum)
}

return outputList