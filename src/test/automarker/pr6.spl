Int[][] in = sequences
Int[] output = []

if (not in.empty) {
	
	Int[] first = in.pop
	Int[] second = in.pop

	Int a
	Int b
	Int c
	Int d
	Int e

	while (not second.empty) {
		a = first.pop
		do output.push(a)

		if (not first.empty) {
			b = first.pop
			c = second.pop
			d = second.pop

			do output.push(b)
			do output.push(c)
			do output.push(d)

			if(not second.empty) {
				e = second.pop
				do output.push(e)
			}
		}
	}
}
return output